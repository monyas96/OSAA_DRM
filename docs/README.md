# Docs

Project documentation for the OSAA DRM application.

## Contents

- **REACT_STREAMLIT_INTERFACE.md** — Contract between the React frontend and the Streamlit dashboard: indicator ID → page mapping, URL/query conventions (`?embed=true`, `?topic=4.1`, etc.), and PostMessage use for iframe height. Required reading when adding/changing policy-brief indicators or the embedding layer.

Other technical or architecture notes can be added here (e.g. an architecture doc such as `react_streamlit_embedding_architecture.md` if it is added to the repo).
